

#pokupki vzuttya v magazini
x <- read.csv("shoes.csv")


plot(x$time~x$amounce) #matrichna diagrama rozsiyuvanna
abline(lm(x$time~x$amounce),col="red")

plot(x$time~x$height) #matrichna diagrama rozsiyuvanna
abline(lm(x$time~x$height),col="red")

plot(x$weight~x$height) #matrichna diagrama rozsiyuvanna
abline(lm(x$weight~x$height),col="red")

x <- read.csv("shoes.csv")
#rahuemo parametry
lm.x <- lm(formula= x$height~x$time,data=x)
lm.x$coefficients
plot(x$height,x$time) #Діаграма розсіювання
bls1<- lm(x$height~x$time,data=x)$coefficients
bls1
abline(lm(x$height~x$time),col="red")
segments(x$height,x$time,x$height,bls1[1]+bls1[2]*x$time)

#Зараз скажемо
#лише, що Age~ztop — це формула, яка вказує, що вiдгуком у нашiй
#моделi є Age, а регресором — ztop; опцiя data вказує фрейм, з якого
#беруться данi для пiдгонки. Результатом виконання функцiї lm() є об’єкт
#складної структури, МНК-оцiнки коефiцiєнтiв регресiї мiстяться у ньому в
#атрибутi $coefficients.